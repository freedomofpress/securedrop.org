document.addEventListener('DOMContentLoaded', () => {
    // Adjust <html> element classes according to JS detection
    document.documentElement.classList.remove('no-js')
    document.documentElement.classList.add('js')
})
