# sha256 as of 2026-06-26
FROM node:26.4.0-trixie@sha256:b46a10d964ad15136ebdf9012142131481caa0697d7a4d4eafe4bbabd818f876 AS node-assets

ARG USERID

RUN getent passwd "${USERID}" || useradd --create-home --no-log-init --uid "${USERID}" sdo_node

USER ${USERID}

CMD npm install && touch .node_complete && npm run start
